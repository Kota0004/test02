# はじめかた — 最初にやること（順番どおりにやれば進みます）

冠水アラートアプリ「みずみち」を、企画から**実データで動く状態**にするまでの手順です。
前提知識なしで追えるように書いてあります。**上から順にやってください。**

- 所要時間の目安: **ステップ0〜2で約30分**、ステップ3が本番（30分〜1時間）
- 全体像は [README.md](README.md)、設計の中身は [docs/](docs/)

> 📱 **iPad・スマホで進める場合は先に [iPadでの進めかた.md](iPadでの進めかた.md) を読んでください。**
> このページはパソコン（ターミナルが使える環境）を前提に書いています。
> iPadでも、GitHub Codespaces を使えばこの手順がそのまま全部実行できます。

> 💡 **用語**
> - **ターミナル**: 文字でコマンドを打つ画面。macOSなら「ターミナル.app」、Windowsなら「PowerShell」。
> - **`$` で始まる行**: ターミナルに打ち込むコマンド（`$` は打たない）。
> - **ローカルサーバ**: 自分のパソコンの中だけで動くWebサーバ。外には公開されません。

---

## ステップ0. 準備（10分）

### 0-1. このフォルダに移動する

```bash
cd <リポジトリを置いた場所>/災害アプリ
```

### 0-2. 必要なライブラリを入れる

```bash
pip install -r tools/requirements.txt
```

> `pip` が見つからないと言われたら `pip3` に読み替えてください。
> 4つのライブラリ（PDF読み取り・画像処理・通信）が入ります。

### 0-3. 今の状態を診断する

```bash
python3 tools/doctor.py
```

**これが一番大事なコマンドです。** 迷ったらいつでもこれを打てば、
「いま何ができて、次に何をすればいいか」が出ます。

こう出れば成功です:

```
── Python ライブラリ ──
  ✅ pdfplumber — ② PDFから冠水箇所の表を読む
  ✅ PIL — ② 標高タイル・降水タイルの画素を読む（Pillow）
  ✅ requests — ②③ 気象庁・国土地理院への通信

── 外部サイトへの到達性 ──
  ✅ www.jma.go.jp — 気象庁（アメダス・ナウキャスト）（HTTP 200）
  ✅ cyberjapandata.gsi.go.jp — 国土地理院（地図タイル・標高タイル）（HTTP 200）
  ...
▶ 次にやること
  1. 国交省のPDFを入手して、...
```

| こう出たら | どうする |
|-----------|---------|
| `❌ pdfplumber が入っていません` | 0-2 をやり直す |
| `❌ www.jma.go.jp に到達できません` | 別のネットワーク（自宅Wi-Fi・スマホのテザリング）で試す。学内ネットワークは制限がかかっていることがあります |
| `❌ 検証に失敗があります` | 表示された内容をそのまま相談してください |

---

## ステップ1. プロトタイプを触る（10分）← あなたの判断が要ります

### 1-1. 起動する

```bash
cd prototype
python3 -m http.server 8000
```

ターミナルに `Serving HTTP on ...` と出たら、ブラウザで **<http://localhost:8000/>** を開きます。
（終わるときは、そのターミナルで `Ctrl+C`）

### 1-2. 触ってみる

1. 左パネルのスライダーを **30 → 50 → 100 mm/h** と動かす。地点の色が変わります。
2. 地点の丸をクリックすると、**この地点の閾値**と**なぜ危険と判定したか**が出ます。
3. **［🔊 音声を試す］** を押すと、実際の通知で流れる読み上げが聞けます。
   （iPhone・iPadでは、一度これを押しておかないと読み上げが鳴りません。
   ブラウザが「利用者の操作なしの発話」を止めるためです）
4. 「現在地の監視を開始」を押すと、位置情報の許可を聞かれます（許可しなくても、
   **地図上を Shift＋クリック** すればそこを現在地として試せます）。

### 1-3. 考えてほしいこと

- **50mm/h のとき、赤や紫になった地点を見て「迂回しよう」と思えるか？**
- レベルの段階（注意/警戒/危険/極めて危険）は多すぎないか、少なくないか
- 通知の文言は、運転中に聞いて意味がわかるか

> 感じたことを教えてください。判定の式（`docs/04`）を調整します。
> できれば家族や友人2〜3人にも見せて、反応を聞いてみてください。

---

## ステップ2. 冠水箇所のPDFを入手してデータにする（15分）

### 2-1. PDFをダウンロードする

国交省 千葉国道事務所のページを開きます:
<https://www.ktr.mlit.go.jp/chiba/chiba_index030.html>

「道路冠水箇所マップ（千葉県内）」のPDFを、この `災害アプリ` フォルダに保存します。
（一覧表と地図の2種類がある場合は、**一覧表（表になっているもの）**を使います）

### 2-2. まず中身が読めるか確認する

```bash
python3 tools/build_spots.py --pdf 一覧表.pdf --dump-text | head -50
```

箇所名や住所が文字として出てくればOKです。

| こう出たら | どうする |
|-----------|---------|
| 住所や箇所名が読める | 2-3 へ進む |
| `(テキストなし)` ばかり | PDFが画像（スキャン）の可能性。**この出力を相談してください** |
| 文字化けしている | 同上。相談してください |

### 2-3. 座標を付ける

```bash
python3 tools/build_spots.py --pdf 一覧表.pdf --area chiba \
    --source "国交省千葉国道事務所 道路冠水箇所マップ（2026-06-30版）" \
    --out data/spots_chiba.json
```

住所を国土地理院の無料サービスで座標に変換します（1件1秒ほど、95件で約2分）。
最後にこう出ます:

```
出力: data/spots_chiba.json  (95 件)
レビュー用CSV: data/spots_chiba.review.csv
要レビュー: 34 件 / 95 件
```

### 2-4. 標高を反映させる（レビュー後にもう一度実行します）

```bash
python3 tools/enrich_dem.py --in data/spots_chiba.json --out data/spots_chiba.json
```

各地点が**周囲よりどれだけ窪んでいるか**を国土地理院の標高データから計算し、
冠水しやすさの閾値に反映します。`dz=-2.10m` のように出れば成功です。

> ⚠️ **dz は座標から計算します。** この時点の座標はまだ機械任せなので、
> **ステップ3のレビューが終わったら、このコマンドをもう一度実行してください。**
> 周囲との高低差が ±6m を超える地点は「座標がずれている疑い」として警告が出ます
> （道路でそんな起伏は現実的でないため）。

---

## ステップ3. 座標を目で確認する（30分〜1時間）← ここが本番

住所から機械的に付けた座標は、**丁目レベルで平気で外れます**。
間違った場所を「危険」と表示するのは、見逃しとは別の種類の害になるので、必ず人が見ます。

### 3-1. レビュー画面を開く

```bash
python3 tools/review_spots.py --spots data/spots_chiba.json
```

ブラウザが自動で開きます（開かなければ <http://127.0.0.1:8765/>）。

### 3-2. 使い方

画面は3つに分かれています。

- **左**: 地点の一覧と進捗（緑=確認済み、赤=除外、灰=未確認）
- **中央**: 地図。青いマーカーがその地点の現在の座標
- **下**: その地点の情報と、確認ボタン

やることは1地点につきこれだけです:

1. 下に出ている**住所**と、地図に出ている**場所**が合っているか見る
2. ズレていたら **マーカーをドラッグ**（または地図をクリック）して正しい位置へ
3. **Enter キー**（または「✔ この位置でOK」）を押す → 次の地点へ自動で進む

| ボタン / キー | 意味 |
|--------------|------|
| **Enter** / ✔ この位置でOK | この座標で確定。確認済みになる |
| ✖ 除外する | この箇所は使わない（重複・廃止など） |
| **→** / **←** | 次 / 前の地点へ |

### 3-3. コツ

- **左上の「写真」に切り替える**と航空写真になります。アンダーパスは、
  道路が下をくぐっているかが写真だとすぐわかります。
- 種別（アンダーパスかポンプ付きか）を直すと、その場で閾値が変わります。
  **自然排水 35mm/h ／ ポンプ有 55mm/h** と差が大きいので、わかる範囲で直してください。
- 変更は**押すたびに自動保存**されます。途中でやめて大丈夫です。
  次に同じコマンドを打てば、続きから再開できます。
- 元のファイルは初回に `data/spots_chiba.json.bak` としてバックアップされます。

> ⏱ **まず10件だけやってみてください。**
> 9割合っていれば残りは流し見でよく、半分外れているならジオコーディングのやり方から
> 見直したほうが早いです。10件やった時点の感触を教えてください。

### 3-4. 終わったら — 標高をもう一度計算する

ターミナルで `Ctrl+C`。確認済み件数が表示されます。

座標が正しくなったので、**標高を計算し直します**（これで閾値が実態に近づきます）。

```bash
python3 tools/enrich_dem.py --in data/spots_chiba.json --out data/spots_chiba.json
```

「高低差が不自然な地点」の警告が減っていれば、レビューが効いています。

```bash
git add -f data/spots_chiba.json    # data/ は既定で除外しているので -f が要ります
git commit -m "千葉県内の冠水注意箇所を座標付きでレビュー・登録"
```

---

## ステップ4. 雨量を取り込んで危険度を出す（5分）

```bash
python3 tools/fetch_amedas.py --spots data/spots_chiba.json --out data/risk_latest.json
```

気象庁のアメダスから**今の雨量**を取ってきて、各地点の危険度を計算します。

```
観測時刻: 2026-08-20T15:30:00+09:00
範囲内の観測点: 24 点
危険度を算出した地点: 95 / 雨量を取れなかった地点: 0
  レベル1 注意: 3 件
  レベル0 平常: 92 件
```

晴れていればほとんど「平常」になります。それが正常です。

| こう出たら | どうする |
|-----------|---------|
| 上のように出た | ✅ 成功。パイプラインが通りました |
| `KeyError` や `JSONDecodeError` | 気象庁のデータ形式が想定と違います。**エラー全文を相談してください**（すぐ直せます） |
| `ConnectionError` | ネットワークの問題。`python3 tools/doctor.py` で確認 |

---

## ステップ5. 自治体に問い合わせる（1時間）← あなたの名前が必要です

### 5-1. 千葉市のシステムを調べる

```bash
python3 tools/probe_endpoints.py --url https://pub.os-alert.info/chiba/devmap --browser
```

（`--browser` を使うには `pip install playwright && playwright install chromium` が必要です。
面倒なら `--browser` なしでも大まかには調べられます）

### 5-2. 問い合わせを送る

[docs/08_自治体連携_打診文案.md](docs/08_自治体連携_打診文案.md) に、
**そのまま使える文案が4通**あります。所属と連絡先を埋めて送ってください。

送る順番:

1. **千葉国道事務所** — 一番大事。「CSVやGeoJSONで原データをもらえないか」を聞きます。
   もらえたら**ステップ3のレビュー作業が丸ごと不要になる可能性があります**
2. **千葉市** — 地下道13カ所のリアルタイム情報。他にない価値があります
3. **気象庁** — データ利用条件の確認
4. **千葉県** — 通行規制情報（フェーズ2で必要）

> 💡 **1通目は、ステップ3を始める前に出しておくのが得です。** 返事を待つ間に
> 他の作業を進められますし、原データがもらえればレビューが不要になります。

---

## 困ったときは

```bash
python3 tools/doctor.py        # いまの状態と次にやることが出ます
./tools/run_tests.sh           # ツール自体が壊れていないか確認（通信不要）
```

エラーが出たら、**そのメッセージをそのままコピーして相談**してください。
とくに次の2つは「起きて当然」なので、遠慮なく投げてください。

- PDFのレイアウトが想定と違って表が読めない
- 気象庁のJSONの形が想定と違う（公式APIではないため仕様変更がありえます）

---

## 全体の流れ（おさらい）

```
ステップ0  環境を整える              10分   pip install → doctor.py
ステップ1  プロトタイプを触る        10分   あなたの判断が要る
ステップ2  PDF → 座標データ          15分   PDFの入手だけ手作業
ステップ3  座標を目で確認            30分〜 ★ここが本番
ステップ4  雨量を取り込む             5分   1コマンド
ステップ5  自治体に問い合わせる       1時間  あなたの名前が要る
```

ここまで終われば、**千葉県内95箇所の実データが、リアルタイムの雨量で色が変わる**状態になります。
そこから先（予測モード・迂回ルート・スマホアプリ化）は
[docs/06_開発ロードマップ.md](docs/06_開発ロードマップ.md) のフェーズ2以降です。
