# プロジェクトメモ

## 提出者情報

課題・レポートの氏名欄・ヘッダーには以下を使用すること。

- **学籍番号:** 4143
- **氏名:** 二川 航大(ふたがわ こうた)

新しくレポートPDF/答案を作成・再生成するときは、この学籍番号・氏名をヘッダーに記入した状態で出力する。

## リポジトリの方針

- 課題ファイルは内容ごとに `問題/`・`レポート課題/`・`講義ノート/`・`参考資料/`・`レポート/` に分類配置する。
- レポート答案PDFは MathJax(SVG)+ Chromium で `scratchpad/pdfbuild/` から生成している(LaTeX処理系は未インストール)。
